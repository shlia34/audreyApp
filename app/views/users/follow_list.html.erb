<div class = "user-show">
  <h2><%= "#{@user.name}さんのフォロー" %></h2>

  <div class = "users_indexs">
    <% @user.all_following.each do |user| %>
      <%= render :partial => "user",:locals => { user: user } %>
    <% end %>
  </div>

</div>
